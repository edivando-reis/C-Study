/* bool flag = true;
int value = 0;
if (flag)
{
    value = 20;
    Console.WriteLine($"Inside the code block: {value}");
}
value = 10;
Console.WriteLine($"Outside the code block: {value}"); */
/* 
int var1 = 5; // Inicializado antes do bloco if

if (var1 > 0)  // 5 > 0 → Verdadeiro, então o bloco é executado
{
    int var2 = 6;  // Inicializado dentro do bloco if
    var1 = var1 + var2;  // var1 = 5 + 6 → var1 = 11
}

Console.WriteLine(var1);  // Exibe 11
 */

 bool flag = true;
 if (flag) 
 {
    Console.WriteLine(flag);
 }

 bool flag2 = true;
 if (flag2)
    Console.WriteLine(flag2);
  
bool flag3 = true;
if (flag3) Console.WriteLine(flag3);